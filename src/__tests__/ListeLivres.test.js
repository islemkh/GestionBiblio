import React from 'react';
import { render } from '@testing-library/react';
import ListeLivres from '../components/listeLivres/ListeLivres';
import mockLivre from '../mock/mockLivre'

describe("test list livres",()=>{
    test("",()=>{
      /*  // console.log(mockLivre)
       const LivresComponent = render(
        <ListeLivres livres={mockLivre} />
)
        expect(LivresComponent).toMatchSnapshot() */
    })
})